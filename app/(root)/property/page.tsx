import React from 'react';
import Property from '@/app/(root)/property/PropertyList';

const ClientsPage = () => {
    return (
        <div>
            <Property data={[]} isLoading={false} />

        </div>
    )
}

export default ClientsPage
